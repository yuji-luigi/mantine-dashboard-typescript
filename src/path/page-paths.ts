export const ROOT = '/';

export const AUTH = {
  LOGIN: '/login',
  SIGNUP: '/sign-up',
};

export const PATH_AFTER_LOGIN = '/dashboard/home';

export enum PATH_DASHBOARD {
  root = '/dashboard',
  dashboard = '/dashboard/home',
}
